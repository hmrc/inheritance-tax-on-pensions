meta {
  name: Submission List - Success
  type: http
  seq: 5
}

get {
  url: {{ihtp_url}}/24000001IN/submission-list
  body: none
  auth: bearer
}

headers {
  srn: S0000000042
  userName: TestUser
  schemeName: TestScheme
  requestRole: PSA
}

auth:bearer {
  token: {{bearer_token}}
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
