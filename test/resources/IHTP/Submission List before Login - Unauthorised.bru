meta {
  name: Submission List before Login - Unauthorised
  type: http
  seq: 2
}

get {
  url: {{ihtp_url}}/24000001IN/submission-list
  body: none
  auth: none
}

headers {
  srn: S0000000042
  userName: TestUser
  schemeName: TestScheme
  requestRole: PSA
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
